{
  "name": "parabank-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end test suite for ParaBank application using Playwright, Page Object Model, and Allure reporting",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:allure": "playwright test",
    "allure:generate": "npx allure generate allure-results -o allure-report --clean",
    "allure:open": "npx allure open allure-report",
    "allure:report": "npm run test:allure && npm run allure:generate && npm run allure:open",
    "docker:build": "docker compose build",
    "docker:test": "docker compose run --rm playwright",
    "docker:allure": "docker compose run --rm playwright npm run allure:report"
  },
  "keywords": [
    "playwright",
    "e2e",
    "testing",
    "parabank",
    "allure"
  ],
  "author": "Bojan Pavlovic",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^10.3.0",
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.3.3",
    "allure-commandline": "^2.37.0",
    "allure-playwright": "^3.5.0"
  }
}
